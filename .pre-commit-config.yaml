repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.1
    hooks:
      - id: ruff
        args:
          - --fix
          - --exit-zero  # Omit error code on lint failures. TODO: dev only
      - id: ruff-format
